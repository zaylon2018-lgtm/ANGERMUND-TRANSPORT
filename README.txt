ANGERMUND TRANSPORT SUPABASE FIXED VERSION

WHAT WAS FIXED:
- Removed JavaScript name conflict: 'supabase has already been declared'
- Login button now works
- It automatically removes /rest/v1/ from URL if pasted by mistake

SUPABASE URL:
Use only:
https://xxxxx.supabase.co

Do NOT use:
https://xxxxx.supabase.co/rest/v1/

SUPABASE SQL SETUP:
In Supabase > SQL Editor, run:

create table if not exists trips (
  id bigint generated by default as identity primary key,
  created_at timestamp with time zone default now(),
  route text,
  driver text,
  truck text,
  km numeric default 0,
  rate numeric default 0,
  cost numeric default 0,
  income numeric default 0,
  profit numeric default 0
);

alter table trips enable row level security;

create policy "Allow authenticated users all trips"
on trips
for all
to authenticated
using (true)
with check (true);

DEPLOY:
1. Upload this folder to GitHub
2. Netlify > Import from Git
3. Publish directory: public
4. Build command: leave empty
5. Deploy
